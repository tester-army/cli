# Landing page smoke

Goal: verify landing page is reachable and primary CTA is visible.

Steps:

1. Navigate to `<target_url>/`
2. Confirm title or hero references TesterArmy
3. Confirm a primary CTA like `Get Started` or `Sign in` is visible
4. Return pass if all checks succeed
