# Create project flow

Goal: verify user can create a new project from dashboard.

Steps:

1. Open `<target_url>/projects` (or dashboard projects section)
2. Click `New Project`
3. Fill required fields with unique test values
4. Submit creation form
5. Confirm new project appears in list and project page opens
6. Return pass/fail with concrete reason
